const numOne = 1;
const numTwo = 1;

// Comparison of numbers looks at the value.
// console.log(numOne === numTwo);

const classOne = {
  name: "Intro to JavaScript",
};

const classTwo = {
  name: "Intro to JavaScript",
};

// Comparison of objects it looks at the reference.
console.log(classOne === classTwo);
// console.log(JSON.stringify(classOne) === JSON.stringify(classTwo));

// console.log(JSON.stringify(classOne));
